export class Card {
    title: string;
    description: string;
    id: string;

    constructor(_title: string, _description: string, _id: string) {
        this.title = _title;
        this.description = _description;
        this.id = _id;
    }

}